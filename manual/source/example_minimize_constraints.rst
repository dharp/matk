.. _minimize:

Minimize residuals using inequality constraints
-----------------------------------------------

This example demonstrates the calibration of a simple model with inequality constraints using the :func:`minimize <matk.matk.matk.minimize>` function from scipy (`<http://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.minimize.html>`_). 

.. include:: minimize_constraints.rst


